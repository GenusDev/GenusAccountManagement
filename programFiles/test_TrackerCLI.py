import pytest
import AccountManager




def test_startTracker():
    result = AccountManager.LogIn(initials,timeTracked)
    assert  result == "output"

def inc(x):
    return x + 1

def test_answer():
    assert inc(3) == 4





# create a virtual environment
# sign up
# set up a fake user to build your tests around - and call your tests to have it walk through the processes
# TT MS guued s
# save password as an environmental variable

# Would keep the terminal window open.
